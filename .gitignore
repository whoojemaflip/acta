/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# rspec failure tracking
.rspec_status

# locally-built gem artifacts (released via Trusted Publishing)
*.gem

# generated by `bundle install` against per-Rails-version gemfiles;
# CI regenerates them, no value in committing
gemfiles/*.gemfile.lock
